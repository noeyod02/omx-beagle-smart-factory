/**:
  ros__parameters:
    video_device: "http://127.0.0.1:8899/cam/cam-warehouse.mjpg"
    model_path: /home/itec/Downloads/0_beagle_item_640.pt_260807/beagle_item_640.pt
    confidence: 0.1
    target_class: beagle
    # Re-aimed 2026-08-31: the camera was moved during the stock-camera
    # shuffle and the old roi [20,240,320,480] landed on the keyboard, so a
    # carrier parked in plain sight read as an empty bay.
    roi: [130, 0, 390, 170]
    # The warehouse box sits to the right of the bay in this framing.
    part_roi: [420, 40, 640, 420]
    part_class: item
    part_confidence: 0.1
    trigger_on_initial: false
    # 대시보드(백엔드 오케스트레이터)가 지휘를 맡은 뒤로는 이 노드가 스스로
    # 적재를 시작하면 안 된다 — 같은 태스크 매니저에 지휘자가 둘이 되면 goal이
    # 서로를 취소시킨다. 빈 문자열이면 감시·보고만 한다.
    transfer_topic: ""
